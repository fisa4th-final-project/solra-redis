apiVersion: v1
kind: Service
metadata:
  name: redis
spec:
  type: NodePort
  selector:
    statefulset.kubernetes.io/pod-name: redis-0
  ports:
    - port: 6379
      targetPort: 6379
      nodePort: 30079
      protocol: TCP

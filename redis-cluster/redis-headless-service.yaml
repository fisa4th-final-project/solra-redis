apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: redis
spec:
  clusterIP: None  # Headless 설정
  ports:
    - port: 6379
      name: client
    - port: 16379
      name: gossip
  selector:
    app: redis
